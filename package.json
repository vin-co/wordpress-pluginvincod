{
  "name": "wp-vincod-plugin",
  "version": "3.0.0",
  "description": "The « Vincod for WordPress » plugin allows you to instantly create a « Our Wines » section in your WordPress website.",
  "author": "Vinternet",
  "license": "GPL-2.0+",
  "homepage": "http://dev.vincod.com",
  "dependencies": {
    "bootstrap": "^5.2.1"
  },
  "devDependencies": {
    "browser-sync": "^2.28.3",
    "browserify": "^17.0.0",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^8.0.0",
    "gulp-cssnano": "^2.1.3",
    "gulp-plumber": "^1.2.1",
    "gulp-sass": "^5.1.0",
    "gulp-sourcemaps": "^3.0.0",
    "gulp-uglify": "^3.0.2",
    "gulp-util": "^3.0.8",
    "node-sass": "^4.14.1",
    "vinyl-buffer": "^1.0.1",
    "vinyl-source-stream": "^2.0.0"
  },
  "scripts": {
    "build": "npm install && gulp dist",
    "clean": "rm -r node_modules; rm -r assets/css; rm assets/js/vendor.js",
    "init": "npm install && gulp build",
    "refresh": "npm run clean; npm run init",
    "dist": "npm run clean; npm run build"
  }
}
