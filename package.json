{
  "name": "wp-vincod-plugin",
  "version": "2.6.3",
  "description": "The « Vincod for WordPress » plugin allows you to instantly create a « Our Wines » section in your WordPress website.",
  "author": "Vinternet",
  "license": "GPL-2.0+",
  "homepage": "http://dev.vincod.com",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^2.3.1",
    "gulp-concat": "^2.5.2",
    "gulp-cssnano": "^2.1.3",
    "gulp-plumber": "^1.2.1",
    "gulp-rename": "^1.4.0",
    "gulp-sass": "^4.0.2",
    "gulp-sourcemaps": "^1.12.1",
    "gulp-uglify": "^1.2.0",
    "gulp-util": "^3.0.5",
    "wiredep": "^4.0.0"
  },
  "scripts": {
    "build": "npm install && bower install && gulp dist",
    "clean": "rm -r node_modules; rm -r bower_components; rm -r assets/css; rm assets/js/vendor.js",
    "init": "npm install && bower install && gulp build",
    "refresh": "npm run clean; npm run init",
    "dist": "npm run clean; npm run build"
  }
}
